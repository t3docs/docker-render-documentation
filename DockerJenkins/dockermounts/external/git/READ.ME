This would be the default directory where Jenkins running in Docker 
would do it's git clone into.

Target directory from Jeniks view: /mnt/external/git

If adjusted the souce directory for t3docs must also be changed.